# Copy to `.env` (repo root) or `app/.env.local` and fill in real values.
# Never commit the filled-in file — both are gitignored.

# Bright Data Web Unlocker.
# Free hackathon credits: https://brdta.com/battlebotsdev
BRIGHT_API_KEY=your_bright_data_api_token
BRIGHTDATA_ZONE=your_unlocker_zone_name

# OpenAI key for the AI analyst + voice commentary.
OPENAI_API_KEY=your_openai_api_key
